// Mirror of --background in app/globals.css, for the places that can't read
// a CSS variable (viewport metadata, the web manifest). `npm run check:style`
// fails if the two drift.
export const BACKGROUND_COLOR = "#141414";
